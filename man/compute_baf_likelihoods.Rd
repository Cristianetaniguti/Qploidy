% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BAF_distributions.R
\name{compute_baf_likelihoods}
\alias{compute_baf_likelihoods}
\title{Compute BAF Likelihoods}
\usage{
compute_baf_likelihoods(
  baf_vec,
  cn_grid,
  M = 100,
  bw = 0.03,
  plot = FALSE,
  dist = "gaussian",
  reflect = TRUE,
  add_uniform = FALSE,
  uniform_weight = 0.05,
  min_het_frac = 0.05,
  het_range = c(0.2, 0.8)
)
}
\arguments{
\item{baf_vec}{Numeric vector of BAF values for a single window.}

\item{cn_grid}{Integer vector of copy-number states to test (e.g., 2:6).}

\item{M}{Integer. Number of BAF histogram bins on [0,1]. Default 100.}

\item{bw}{Numeric. Bandwidth (SD) of the Gaussian kernels used to generate the BAF comb templates. Default 0.03.}

\item{plot}{Logical. If TRUE, return a ggplot object showing observed and template distributions with likelihood and probability text for each CN. Default FALSE.}

\item{dist}{Character. Distribution type for template peaks: "gaussian" (default), "beta", "beta_binomial", or "negative_binomial".}

\item{reflect}{Logical. If TRUE (default), apply reflection for continuous
kernels to keep mass within [0,1] (useful for gaussian).}

\item{add_uniform}{Logical. If TRUE, add a uniform noise component to the
mixture before renormalization. Default FALSE.}

\item{uniform_weight}{Numeric in [0,1]. Mixture weight of the uniform
component when \code{add_uniform = TRUE}. Default 0.05.}

\item{min_het_frac}{Numeric in [0,1]. Threshold for the fraction of BAF values in \code{het_range} considered heterozygous. If the observed heterozygous fraction exceeds this value, CN=1 is excluded from \code{cn_grid}, as a meaningful proportion of heterozygous loci makes haploid (CN=1) implausible. Default 0.05.}

\item{het_range}{Numeric vector of length 2. BAF interval used to define heterozygous loci (default \code{c(0.2, 0.8)}). Values outside this range are treated as homozygous for the purpose of the \code{min_het_frac} filter.}
}
\value{
A list containing:
  \item{ll_vec}{Vector of log-likelihoods (n_cn), one per CN template.}
  \item{prob_vec}{Vector of probabilities (n_cn), softmax-transformed from log-likelihoods.}
  \item{plot}{Optional ggplot object if plot=TRUE.}
}
\description{
Given a vector of BAF values and a vector of tested copy numbers (cn_grid),
this function creates BAF templates for each CN, computes the likelihood of the observed BAF
histogram under each template, and returns a vector of likelihoods (one per CN state).
}
\details{
Uses the generate_baf_template and baf_log_likelihood helpers from Qploidy.
}
\author{
Cristiane Taniguti
}
