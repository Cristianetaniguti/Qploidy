% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmm_utils.R
\name{define_z_limits}
\alias{define_z_limits}
\title{Initialize monotonic z-score means for HMM ploidy states}
\usage{
define_z_limits(
  z,
  z_window,
  cn_grid,
  exp_ploidy,
  z_range = NULL,
  verbose = FALSE,
  z_range_out = TRUE
)
}
\arguments{
\item{z}{Numeric vector. Window-level z-scores.}

\item{cn_grid}{Integer vector. Copy-number states to consider.}

\item{exp_ploidy}{Numeric. Expected ploidy value (center of ramp).}

\item{z_range}{Numeric. Padding added to min/max z for ramp initialization. If NULL, estimated from z interquartile range.}

\item{verbose}{Logical. If TRUE, prints estimated z_range. Default FALSE.}

\item{z_range_out}{Logical. If TRUE, expand range (min - z_range, max + z_range). If FALSE, reduce range (min + z_range, max - z_range).}
}
\value{
Named numeric vector of z-score means (mu) for each ploidy state in cn_grid.
}
\description{
Computes a monotonic ramp of z-score means (mu) for each ploidy state, centered on the expected ploidy, with padding to avoid boundary collapse.
Used for initializing HMM emission parameters in ploidy estimation.
}
\details{
The ramp is constructed as: mu_c = z_mean + step * (c - exp_ploidy), where step is chosen so the lowest and highest ploidy states fit within the padded z range.
Ensures monotonic initialization for EM fitting.
}
\keyword{internal}
