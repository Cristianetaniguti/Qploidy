% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BAF_distributions.R
\name{generate_baf_template}
\alias{generate_baf_template}
\title{Build a polyploid BAF comb template for a given copy number}
\usage{
generate_baf_template(
  cn,
  M = 101,
  bw = 0.03,
  floor_eps = 1e-08,
  dist = c("gaussian", "beta", "beta_binomial", "negative_binomial"),
  reflect = TRUE,
  add_uniform = FALSE,
  uniform_weight = 0.05
)
}
\arguments{
\item{cn}{Integer. Copy number (e.g., 2 for diploid, 4 for tetraploid).}

\item{M}{Integer. Number of bins over [0,1]. Default is 101.}

\item{bw}{Numeric. Bandwidth / concentration control (interpretation depends
on \code{dist}). For \code{gaussian}, it is sd on the BAF scale.
For \code{beta}, it controls concentration around the mode.
For \code{beta_binomial} and \code{negative_binomial}, it controls optional
smoothing applied on the x-grid.}

\item{floor_eps}{Numeric. Small positive value added before renormalization.}

\item{dist}{Character. One of \code{"gaussian"}, \code{"beta"},
\code{"beta_binomial"}, \code{"negative_binomial"}.}

\item{reflect}{Logical. If TRUE (default), apply reflection for continuous
kernels to keep mass within [0,1] (useful for gaussian).}

\item{add_uniform}{Logical. If TRUE, add a uniform noise component to the
mixture before renormalization. Default FALSE.}

\item{uniform_weight}{Numeric in [0,1]. Mixture weight of the uniform
component when \code{add_uniform = TRUE}. Default 0.05.}
}
\value{
Numeric vector of length \code{M} summing to 1.
}
\description{
Constructs a discrete BAF density template on [0,1] for a given copy number
\code{c}, by placing kernels at genotype fractions \eqn{d/c} for
\eqn{d=0,...,c}. Binomial weights are used for relative peak heights.
}
\details{
Optionally, the template can be extended with a uniform noise component to
capture "off-comb" BAF values (e.g., mapping bias, noisy loci).
}
\author{
Cristiane Taniguti
}
