% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_raw.R
\name{select_best_raw_model}
\alias{select_best_raw_model}
\title{Select the Best BAF Model from Raw Allele-Count Data}
\usage{
select_best_raw_model(
  data,
  sample,
  cn_grid,
  dists = c("gaussian", "beta", "beta_binomial", "negative_binomial"),
  reflect = TRUE,
  bw_grid = c(0.02, 0.03, 0.04),
  add_uniform_grid = FALSE,
  uniform_weight_grid = c(0.01, 0.03, 0.05, 0.1, 0.15),
  M = 100,
  plot = FALSE,
  param_count = NULL,
  count_grid_as_params = TRUE,
  min_het_frac = 0.05,
  het_range = c(0.2, 0.8)
)
}
\arguments{
\item{data}{A `data.frame` containing at minimum columns `SampleName` and
`ratio`. Additional columns (e.g. `R`, `MarkerName`) are ignored.}

\item{sample}{Character string. The value of `SampleName` to select.}

\item{cn_grid}{Integer vector of copy-number states to test (e.g. `2:6`).}

\item{dists}{Character vector of distribution families to test. Passed to
[select_best_baf_model()]. Default:
`c("gaussian", "beta", "beta_binomial", "negative_binomial")`.}

\item{reflect}{Logical. Apply boundary reflection for continuous kernels.
Default `TRUE`.}

\item{bw_grid}{Numeric vector of bandwidth values to search over. Default
`c(0.02, 0.03, 0.04)`.}

\item{add_uniform_grid}{Logical vector. Whether to include a uniform noise
component. Default `FALSE`.}

\item{uniform_weight_grid}{Numeric vector of uniform mixture weights to try.
Default `c(0.01, 0.03, 0.05, 0.10, 0.15)`.}

\item{M}{Integer. Number of BAF histogram bins. Default `100`.}

\item{plot}{Logical. If `TRUE`, a ggplot for the best model is included in
the returned object. Default `FALSE`.}

\item{param_count}{Optional named integer vector of free-parameter counts per
distribution, used for BIC penalisation. Default `NULL` (zero for all).}

\item{count_grid_as_params}{Logical. Add +1 BIC penalty per tuned
hyperparameter. Default `TRUE`.}

\item{min_het_frac}{Numeric. Minimum heterozygous fraction required to
exclude CN=1 from `cn_grid`. Default `0.05`.}

\item{het_range}{Numeric vector of length 2. BAF interval defining
heterozygous loci. Default `c(0.2, 0.8)`.}
}
\value{
An object of class `selected_BAF_model` as returned by
  [select_best_baf_model()].
}
\description{
Convenience wrapper around [select_best_baf_model()] that accepts the raw
`data` data.frame used by [plot_raw()], filters it to a single sample, and
passes the `ratio` column as the `baf_vec` argument. All other arguments are
forwarded verbatim to [select_best_baf_model()].
}
\seealso{
[select_best_baf_model()], [plot_raw()]
}
