test_that("From standardized data", {

  # input_file <- "~/Documents/species/hydrangea/WGS/Qploidy/BAM_counts/standardized_dp10-30.tsv.gz"
  # data_standardized <- read_qploidy_standardization(input_file)
  #
  # ggplot(data_standardized$data, aes(x = R)) +
  #   geom_histogram() +
  #   coord_cartesian(xlim = c(0, 500))
  #
  # ggplot(data_standardized$data, aes(x = R)) +
  #   geom_histogram(bins = 30) +
  #   scale_x_continuous(limits = c(0, 100)) +
  #   geom_vline(xintercept = c(10,30), linetype = "dashed", color = "red")
  #
  #  hist(data_standardized$data$R)
  #
  # plot_qploidy_standardization(data_standardized, sample = "G076")

})
